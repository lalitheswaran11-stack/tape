{
  "name": "@tape/feed",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Deterministic seeded market-data feed: WebSocket stream + REST snapshot + fault injection.",
  "scripts": {
    "dev": "tsx src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@lalithesh-star/tape-core": "workspace:^",
    "@types/node": "^22.5.0",
    "@types/ws": "^8.5.12",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
